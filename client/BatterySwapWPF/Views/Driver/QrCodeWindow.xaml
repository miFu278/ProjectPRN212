<Window x:Class="BatterySwapWPF.Views.Driver.QrCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="QR Code" 
        Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="20">
            <StackPanel>
                <TextBlock Text="Booking QR Code" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Show this QR code at the station" FontSize="13" Foreground="#E0FFFFFF" Margin="0,5,0,0"/>
            </StackPanel>
        </materialDesign:ColorZone>

        <!-- QR Code -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="20">
            <Border Background="White" 
                   BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                   BorderThickness="3"
                   CornerRadius="8"
                   Padding="20"
                   HorizontalAlignment="Center">
                <Image x:Name="QrImage" 
                      Width="300" Height="300"
                      Stretch="Uniform"/>
            </Border>

            <TextBlock x:Name="BookingIdText"
                      FontSize="24" FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,20,0,0"
                      Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <TextBlock x:Name="StationText"
                      FontSize="14"
                      HorizontalAlignment="Center"
                      Foreground="Gray"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="2" 
               Content="CLOSE"
               Click="Close_Click"
               Style="{StaticResource MaterialDesignFlatButton}"
               Margin="20"
               Height="40"/>
    </Grid>
</Window>
